@{
    ViewData["Title"] = "Agent Toor - Dashboard";
}

<!-- Hero Section Compacto -->
<section class="hero-section-compact">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <div class="hero-content">
                    <h1 class="hero-title">
                        Bem-vindo ao <span class="text-success">Agent Toor</span>
                    </h1>
                    <p class="hero-subtitle">
                        A plataforma definitiva para agentes de viagem criarem propostas impactantes e acompanharem o sucesso de cada projeto.
                    </p>
                    <div class="cta-buttons">
                        <a href="/Proposta/Criar" class="btn btn-success btn-lg me-3">
                            <i class="fas fa-plus me-2"></i>
                            Criar Nova Proposta
                        </a>
                        <a href="/Proposta" class="btn btn-outline-primary btn-lg">
                            <i class="fas fa-list me-2"></i>
                            Ver Minhas Propostas
                        </a>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="text-center">
                    <div class="hero-graphic">
                        <i class="fas fa-globe-americas"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="features-section">
    <div class="container">
        <div class="row text-center mb-5">
            <div class="col-lg-8 mx-auto">
                <h2 class="section-title">Por que escolher o Agent Toor?</h2>
                <p class="section-subtitle">Transforme a forma como você cria e gerencia propostas de viagem</p>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-magic"></i>
                    </div>
                    <h4>Criação Intuitiva</h4>
                    <p>Interface moderna e fácil de usar. Crie propostas profissionais em minutos.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <h4>Analytics Avançado</h4>
                    <p>Acompanhe em tempo real como seus clientes interagem com as propostas.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-share-alt"></i>
                    </div>
                    <h4>Compartilhamento Fácil</h4>
                    <p>Links públicos seguros e compartilhamento direto por WhatsApp e email.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-images"></i>
                    </div>
                    <h4>Galeria Rica</h4>
                    <p>Adicione fotos incríveis dos destinos e crie uma experiência visual impactante.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h4>Totalmente Responsivo</h4>
                    <p>Funciona perfeitamente em desktop, tablet e smartphone.</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-6">
                <div class="feature-card text-center">
                    <div class="feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h4>Seguro e Confiável</h4>
                    <p>Controle total sobre a privacidade das propostas com links seguros.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Quick Actions -->
<section class="quick-actions-section">
    <div class="container">
        <div class="row text-center mb-4">
            <div class="col-12">
                <h2>Ações Rápidas</h2>
                <p>Acesse rapidamente as principais funcionalidades</p>
            </div>
        </div>
        <div class="row g-4">
            <div class="col-lg-3 col-md-6">
                <a href="/Proposta/Criar" class="quick-action-card">
                    <div class="quick-action-icon bg-success">
                        <i class="fas fa-plus"></i>
                    </div>
                    <h5>Nova Proposta</h5>
                    <p>Criar uma nova proposta de viagem</p>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="/Proposta" class="quick-action-card">
                    <div class="quick-action-icon bg-primary">
                        <i class="fas fa-list"></i>
                    </div>
                    <h5>Minhas Propostas</h5>
                    <p>Gerenciar propostas existentes</p>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="/PropostaAnalyticsData/Dashboard" class="quick-action-card">
                    <div class="quick-action-icon bg-info">
                        <i class="fas fa-chart-bar"></i>
                    </div>
                    <h5>Analytics</h5>
                    <p>Ver relatórios e estatísticas</p>
                </a>
            </div>
            <div class="col-lg-3 col-md-6">
                <a href="/Usuario" class="quick-action-card">
                    <div class="quick-action-icon bg-warning">
                        <i class="fas fa-users"></i>
                    </div>
                    <h5>Usuários</h5>
                    <p>Gerenciar usuários do sistema</p>
                </a>
            </div>
        </div>
    </div>
</section>

@section Styles {
    <style>
        /* Hero Section Compacto */
        .hero-section-compact {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 3rem 0; /* REDUZIDO: era 80px (5rem), agora 48px (3rem) */
            position: relative;
            overflow: hidden;
            margin: -2rem -15px 2rem -15px; /* REDUZIDO: margin bottom era 3rem, agora 2rem */
        }

        .hero-section-compact::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="white" opacity="0.1"><polygon points="1000,100 1000,0 0,100"/></svg>') no-repeat bottom;
            background-size: cover;
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero-title {
            font-size: 2.5rem; /* REDUZIDO: era 3.5rem, agora 2.5rem */
            font-weight: 900;
            margin-bottom: 1rem; /* REDUZIDO: era 1.5rem, agora 1rem */
            line-height: 1.2;
        }

        .hero-subtitle {
            font-size: 1.1rem; /* REDUZIDO: era 1.25rem, agora 1.1rem */
            opacity: 0.9;
            margin-bottom: 1.5rem; /* REDUZIDO: era 2.5rem, agora 1.5rem */
            font-weight: 300;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .hero-graphic {
            font-size: 6rem; /* REDUZIDO: era 12rem, agora 6rem */
            opacity: 0.3;
            animation: float 3s ease-in-out infinite;
        }

        @@keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-15px); } /* REDUZIDO: era -20px, agora -15px */
        }

        /* Features Section */
        .features-section {
            padding: 4rem 0; /* REDUZIDO: era 80px (5rem), agora 64px (4rem) */
            background: #f8f9fa;
        }

        .section-title {
            font-size: 2.25rem; /* REDUZIDO: era 2.5rem, agora 2.25rem */
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 1rem;
        }

        .section-subtitle {
            font-size: 1rem; /* REDUZIDO: era 1.1rem, agora 1rem */
            color: #6c757d;
            margin-bottom: 0;
        }

        .feature-card {
            background: white;
            padding: 2rem 1.5rem; /* REDUZIDO: era 2.5rem 2rem, agora 2rem 1.5rem */
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            height: 100%;
            border: 1px solid #e9ecef;
        }

        .feature-card:hover {
            transform: translateY(-8px); /* REDUZIDO: era -10px, agora -8px */
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }

        .feature-icon {
            width: 60px; /* REDUZIDO: era 80px, agora 60px */
            height: 60px; /* REDUZIDO: era 80px, agora 60px */
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.25rem; /* REDUZIDO: era 1.5rem, agora 1.25rem */
            color: white;
            font-size: 1.5rem; /* REDUZIDO: era 2rem, agora 1.5rem */
        }

        .feature-card h4 {
            font-size: 1.25rem; /* REDUZIDO: era 1.4rem, agora 1.25rem */
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 0.75rem; /* REDUZIDO: era 1rem, agora 0.75rem */
        }

        .feature-card p {
            color: #6c757d;
            line-height: 1.6;
            margin-bottom: 0;
            font-size: 0.95rem; /* REDUZIDO: padrão era 1rem, agora 0.95rem */
        }

        /* Quick Actions */
        .quick-actions-section {
            padding: 4rem 0; /* REDUZIDO: era 80px (5rem), agora 64px (4rem) */
            background: white;
        }

        .quick-action-card {
            display: block;
            background: white;
            padding: 1.5rem; /* REDUZIDO: era 2rem, agora 1.5rem */
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            text-decoration: none;
            color: inherit;
            text-align: center;
            height: 100%;
            border: 1px solid #e9ecef;
        }

        .quick-action-card:hover {
            transform: translateY(-4px); /* REDUZIDO: era -5px, agora -4px */
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
            text-decoration: none;
            color: inherit;
        }

        .quick-action-icon {
            width: 50px; /* REDUZIDO: era 60px, agora 50px */
            height: 50px; /* REDUZIDO: era 60px, agora 50px */
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            color: white;
            font-size: 1.25rem; /* REDUZIDO: era 1.5rem, agora 1.25rem */
        }

        .quick-action-card h5 {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 0.5rem;
            font-size: 1.1rem; /* REDUZIDO: padrão era maior, agora 1.1rem */
        }

        .quick-action-card p {
            color: #6c757d;
            font-size: 0.85rem; /* REDUZIDO: era 0.9rem, agora 0.85rem */
            margin-bottom: 0;
        }

        /* Responsivo */
        @@media (max-width: 768px) {
            .hero-section-compact {
                padding: 2rem 0; /* REDUZIDO: era 60px, agora 32px */
                margin: -1rem -15px 1.5rem -15px; /* REDUZIDO: margin bottom */
            }

            .hero-title {
                font-size: 2rem; /* REDUZIDO: era 2.5rem, agora 2rem */
            }

            .hero-subtitle {
                font-size: 1rem; /* REDUZIDO: era 1.1rem, agora 1rem */
            }

            .hero-graphic {
                font-size: 4rem; /* REDUZIDO: era 8rem, agora 4rem */
                margin-top: 1rem;
            }

            .cta-buttons {
                justify-content: center;
                flex-direction: column;
                align-items: center;
            }

            .cta-buttons .btn {
                width: 100%;
                max-width: 300px;
            }

            .features-section,
            .quick-actions-section {
                padding: 3rem 0; /* REDUZIDO: era 60px, agora 48px */
            }

            .section-title {
                font-size: 1.75rem; /* REDUZIDO: era 2rem, agora 1.75rem */
            }

            .feature-card {
                margin-bottom: 1.5rem; /* REDUZIDO: era 2rem, agora 1.5rem */
                padding: 1.5rem 1rem; /* REDUZIDO padding mobile */
            }
        }

        /* Animações mais suaves */
        .hero-content,
        .feature-card,
        .quick-action-card {
            animation: fadeInUp 0.6s ease-out; /* REDUZIDO: era 0.8s, agora 0.6s */
        }

        .feature-card:nth-child(1) { animation-delay: 0.1s; }
        .feature-card:nth-child(2) { animation-delay: 0.15s; } /* REDUZIDO delays */
        .feature-card:nth-child(3) { animation-delay: 0.2s; }
        .feature-card:nth-child(4) { animation-delay: 0.25s; }
        .feature-card:nth-child(5) { animation-delay: 0.3s; }
        .feature-card:nth-child(6) { animation-delay: 0.35s; }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px); /* REDUZIDO: era 30px, agora 20px */
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
}

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Contador animado (se você quiser adicionar estatísticas)
            function animateNumbers() {
                const numbers = document.querySelectorAll('.stat-number');
                numbers.forEach(number => {
                    const finalNumber = parseInt(number.textContent);
                    let currentNumber = 0;
                    const increment = finalNumber / 20; // REDUZIDO: era 30, agora 20

                    const timer = setInterval(() => {
                        currentNumber += increment;
                        if (currentNumber >= finalNumber) {
                            currentNumber = finalNumber;
                            clearInterval(timer);
                        }
                        number.textContent = Math.floor(currentNumber);
                    }, 40); // REDUZIDO: era 50ms, agora 40ms
                });
            }

            // Smooth scroll para links internos
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });
        });
    </script>
}